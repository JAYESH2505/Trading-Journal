# Trading-Journal
A single-file, browser-based trading journal that tracks rule adherence separately from trade outcome — because a loss with good process isn't a failure, and a win with broken rules isn't one either. No backend, no account, runs entirely client-side.
